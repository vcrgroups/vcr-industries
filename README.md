
# VCR Group of Industries — Next.js (App Router) site

This is a ready-to-deploy Next.js 14 app-router project tailored for VCR Group of Industries.
Replace the placeholder images in `public/` with your brand images.

## To run locally
1. `npm install`
2. `npm run dev`
3. Open http://localhost:3000

## To deploy on Vercel
Push this repo to GitHub and Vercel will detect and deploy automatically.
